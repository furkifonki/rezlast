{
  "name": "rezervasyon-uygulamasi",
  "version": "1.0.0",
  "private": true,
  "description": "Rezervasyon uygulaması - Admin panel + Mobil app",
  "scripts": {
    "start": "cd mobile-app && CI=1 npm run start:lan",
    "admin": "cd admin-panel && npm run dev",
    "admin:build": "cd admin-panel && npm run build",
    "mobile": "cd mobile-app && CI=1 npm run start:lan",
    "mobile:lan": "cd mobile-app && CI=1 npm run start:lan"
  }
}
